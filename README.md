Deprecated for [coler](https://github.com/coletl/coler). I'll miss the name, but it wasn't obviously a personal package.

### To install
```R
# install.packages("devtools")
devtools::install_github("coletl/easyr")
```
